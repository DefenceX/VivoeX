name: "Build : Cyclone"

on:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: read
  pull-requests: read

jobs:
  check-links:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Apt Update
        run: sudo apt-get update
